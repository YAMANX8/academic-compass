import { configureStore } from '@reduxjs/toolkit';

//here we import the slices from the feartures folder
// import playerReducer from './features/playerSlice';

export const store = configureStore({
  reducer: {
  },
});
